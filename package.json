{
  "name": "redis-queue-worker",
  "version": "0.1.0",
  "description": "A worker that can perform tasks after reading from a redis list",
  "main": "bin/redis-queue-worker.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "David House",
  "license": "MIT",
  "dependencies": {
    "chalk": "^2.4.2",
    "clear": "^0.1.0",
    "figlet": "^1.2.1",
    "rc": "^1.2.8",
    "redis": "^2.8.0"
  }
}
